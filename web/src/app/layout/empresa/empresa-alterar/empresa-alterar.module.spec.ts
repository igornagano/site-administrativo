import { EmpresaAlterarModule } from './empresa-alterar.module';

describe('EmpresaAlterarModule', () => {
    let empresaAlterarModule: EmpresaAlterarModule;

    beforeEach(() => {
        empresaAlterarModule = new EmpresaAlterarModule();
    });

    it('should create an instance', () => {
        expect(EmpresaAlteraroModule).toBeTruthy();
    });
});
